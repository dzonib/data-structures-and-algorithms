package anagram

func IsAnagram(s string, s2 string) bool {

	// make rune arrays, so we can iterate over each character, so we can iterate over them

	// make maps, so we can add character (string) and number of how much time it does exist in a string (int)

	// iterate over the rune arrays, and add key, and values to the holder maps

	// compare the two maps and return true if the number of characters matches, or false if it does not

	return false
}
